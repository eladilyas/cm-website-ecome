// Commerce-wide constants — VAT rate, shipping defaults, currency tag.
// Shared between the customer checkout UI and the server-side order
// service so the displayed totals match what gets persisted.

export const VAT_RATE = 0.2; // 20% Moroccan VAT
export const DEFAULT_CURRENCY = "MAD";
