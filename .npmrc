# Tolerate Next.js 16.2.9's own internally-inconsistent peer constraint
# on @swc/helpers (it depends on 0.5.15 and peer-declares >=0.5.17,
# which `npm ci` refuses in strict mode). Drop this entry once we move
# to a Next release where the upstream contradiction is fixed.
legacy-peer-deps=true
